删库跑路，血的教训，慎用
 rm -rf /*
 /表示根目录
 /*表示根目录下的所有文件，

 删除当前目录下的所有文件及目录（感觉这个比较安全）
 rm -r *
---------------------------------------------------------
cat

显示编号
cat -n test

查看前5行记录
cat test | head -n 5

查看后5行记录
cat test | tail -n 5

查看容量
df -h


